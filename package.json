{
  "name": "marianas-treats",
  "version": "1.0.0",
  "description": "Mariana's Treats — NJ independent coffee & bakery route planner",
  "scripts": {
    "build": "node build.js",
    "dev": "GOOGLE_MAPS_API_KEY=your_key_here node build.js && open dist/index.html"
  },
  "engines": {
    "node": ">=16"
  }
}
