name = "marianas-treats-yelp"
main = "worker.js"
compatibility_date = "2024-01-01"

# After deploying, connect this worker to your Pages site via the dashboard:
# Workers & Pages → marianas-treats → Settings → Functions → KV namespace bindings
# OR simply add the worker route manually in the Worker's Triggers tab.
#
# Set your Yelp key as a secret — run this after deploying:
#   wrangler secret put YELP_API_KEY
